<div class="tab {{tab.cssClass}}" data-tab="attributes" data-group="primary">
    <h1>{{actor.name}}</h1>
    
    <fieldset>
        <legend>God</legend>
        {{#if god}}
            <div class="summary-row">
                <img class="item-img" src="{{god.img}}" data-action="openItem" data-item-id="{{god.id}}" alt="{{god.name}}">
                <span class="item-name" data-action="openItem" data-item-id="{{god.id}}">{{god.name}}</span>
                <button type="button" class="icon-btn" data-action="removeGodArchetype" data-item-id="{{god.id}}" title="Remove">
                    <i class="fas fa-trash"></i>
                </button>
            </div>
        {{else}}
            <p>Drag a God item here</p>
        {{/if}}
    </fieldset>

    <fieldset>
        <legend>Archetype</legend>
        {{#if archetype}}
            <div class="summary-row">
                <img class="item-img" src="{{archetype.img}}" data-action="openItem" data-item-id="{{archetype.id}}" alt="{{archetype.name}}">
                <span class="item-name" data-action="openItem" data-item-id="{{archetype.id}}">{{archetype.name}}</span>
                <button type="button" class="icon-btn" data-action="removeGodArchetype" data-item-id="{{archetype.id}}" title="Remove">
                    <i class="fas fa-trash"></i>
                </button>
            </div>
        {{else}}
            <p>Drag an Archetype item here</p>
        {{/if}}
    </fieldset>

    <fieldset>
        <legend>Resources</legend>

        <div class="resource-cards">
            {{#each resourceBars}}
                <div class="resource-card">
                    <span class="resource-name">{{this.key}}</span>
                    <div class="resource-numbers">
                        <input type="number" name="system.resources.{{this.key}}.value" value="{{this.value}}" min="0">
                        <span>/ {{this.max}} </span>
                    </div>
                    <div class="resource-bar">
                        <div class="resource-bar-fill" style="width: {{this.pct}}%;"></div>
                    </div>
                </div>
            {{/each}}
        </div>
    </fieldset>

    <fieldset>
        <legend>Attributes</legend>

        {{#each attributeRows}}
            <div class="score-row">
                <span 
                    class="score-label" 
                    data-action="openRoll"
                    data-group="attributes"
                    data-key="{{this.key}}"
                >{{this.label}}</span>

                <div class="score-dots">
                    {{#each this.dots}}
                        <span
                            class="score-dot {{#if this}}filled{{/if}}"
                            data-action="setScore"
                            data-group="attributes"
                            data-key="{{../key}}"
                            data-value="{{add @index 1}}"
                        ></span>
                    {{/each}}
                </div>
            </div>
        {{/each}}
    </fieldset>

   <fieldset>
        <legend>Skills</legend>

        {{#each skillRows}}
            <div class="score-row">
                <span 
                class="score-label"
                data-action="openRoll"
                data-group="skills"
                data-key="{{this.key}}"
                >{{this.label}}</span>

                <div class="score-dots">
                    {{#each this.dots}}
                        <span
                            class="score-dot {{#if this}}filled{{/if}}"
                            data-action="setScore"
                            data-group="skills"
                            data-key="{{../key}}"
                            data-value="{{add @index 1}}"
                        ></span>
                    {{/each}}
                </div>
            </div>
        {{/each}}
    </fieldset>

    <fieldset>
        <legend>Other</legend>
        <div class="stat-cards">
            <div class="stat-card">
                <span class="resource-name">Experience</span>
                <div class="xp-numbers">
                    <div class="xp-field">
                        <label>Current</label>
                        <input type="number" name="system.experience.current" value="{{system.experience.current}}" min="0">
                    </div>
                    <div class="xp-field">
                        <label>Total</label>
                        <input type="number" name="system.experience.total" value="{{system.experience.total}}" min="0">
                    </div>
                </div>
            </div>

            <div class="stat-card">
                <span class="resource-name">Divine Help</span>
                <input type="number" name="system.divineHelpPoints" value="{{system.divineHelpPoints}}" min="0" class="stat-single-input">
            </div>

            <div class="stat-card">
                <span class="resource-name">Dogma Breaks</span>
                <div class="score-dots score-dots-center">
                    {{#each dogmaDots}}
                        <span
                            class="score-dot {{#if this}}filled{{/if}}"
                            data-action="setDogmaBreaks"
                            data-value="{{add @index 1}}"
                        ></span>
                    {{/each}}
                </div>
            </div>
        </div>
    </fieldset>
</div>